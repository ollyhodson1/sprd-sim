# sPRD - Student Practice Record Document (refactored)

This is the same working sPRD prototype, split into smaller files so it is easier to extend.

## Run locally

```bash
npm install
npm run dev
```

## Build for hosting

```bash
npm run build
```

The production files will be created in `dist/`. Data is saved in the browser using `localStorage`.
